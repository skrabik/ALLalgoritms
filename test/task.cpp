#include <bits/stdc++.h>

using namespace std;

int main () {

    int n = 1000;
    vector<vector<int>> ptr;
    
    for (auto i: ptr) {
        for (auto j: i) {
            cout << i << ' ';
        } cout << endl;
    }
 
}